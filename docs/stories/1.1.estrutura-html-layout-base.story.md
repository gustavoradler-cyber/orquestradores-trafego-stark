---
story_id: "1.1"
title: "Estrutura HTML e Layout Base"
epic: 1
status: Done
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["checklist-runner", "browser"]
created_by: "@sm"
created_at: "2026-05-08"
source_prd: "docs/prd/calculadora-web.md"
---

# Story 1.1: Estrutura HTML e Layout Base

## Status

Done

## Executor Assignment

- **Executor:** @dev
- **Quality Gate:** @qa
- **Quality Gate Tools:** checklist-runner, browser (abertura no browser sem erros)

## Story

**As a** usuário,  
**I want** ver a calculadora com display e teclado numérico dispostos corretamente na tela,  
**so that** eu possa orientar minha interação antes mesmo de clicar em algo.

## Acceptance Criteria

1. O arquivo `index.html` existe na raiz com estrutura HTML5 semântica válida
2. O `<head>` inclui `<link>` para Google Fonts (Inter ou Outfit) e referências ao `style.css` e `script.js`
3. O layout apresenta dois blocos lado a lado: bloco da calculadora (display + teclado) à esquerda e bloco de histórico à direita
4. O bloco da calculadora contém: área de display superior (expressão) e área de display inferior (resultado), e grid de botões com dígitos 0-9, operadores (+, -, ×, ÷), igual (=), ponto decimal (.), limpar (C) e apagar (⌫)
5. O bloco de histórico contém: título "Histórico", lista vazia com placeholder e botão "Limpar histórico"
6. A página abre no browser sem erros no console (zero erros JavaScript, zero recursos 404)
7. A paleta de cores base e CSS Custom Properties estão definidas em `style.css` para dark mode (padrão) e light mode

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type:** Frontend  
**Secondary Type(s):** N/A  
**Complexity:** Low

### Specialized Agent Assignment

**Primary Agents:**
- @dev — implementação e pre-commit review

**Supporting Agents:**
- @qa — validação dos ACs no browser

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): Verificar HTML5 semântico válido, zero erros no console
- [ ] Pre-PR (@devops): Run antes de criar pull request

### Self-Healing Configuration

**Expected Self-Healing:**
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutes
- Severity Filter: CRITICAL, HIGH

**Predicted Behavior:**
- CRITICAL issues: auto_fix (max 2 iterations)
- HIGH issues: document_as_debt

### CodeRabbit Focus Areas

**Primary Focus:**
- Semântica HTML5: uso correto de `<main>`, `<section>`, `<header>`, `<button>`
- Acessibilidade: atributos `aria-label` nos botões, contraste mínimo WCAG AA

**Secondary Focus:**
- CSS Custom Properties corretamente definidas para ambos os temas
- Ausência de inline styles desnecessários

## Tasks / Subtasks

- [x] Task 1: Criar estrutura de arquivos do projeto (AC: 1, 2)
  - [x] 1.1 Criar `index.html` na raiz do projeto com doctype HTML5
  - [x] 1.2 Criar `style.css` referenciado no `<head>`
  - [x] 1.3 Criar `script.js` referenciado antes do `</body>` (placeholder com comentário)
  - [x] 1.4 Adicionar `<link>` para Google Fonts (Inter, weights: 300, 400, 700)

- [x] Task 2: Implementar layout de duas colunas (AC: 3)
  - [x] 2.1 `.app-container` com Flexbox lado a lado (calculadora | histórico)
  - [x] 2.2 Bloco calculadora como `<section class="calculator">` semântica

- [x] Task 3: Estrutura do display duplo (AC: 4)
  - [x] 3.1 `<div id="expression-display">` com `aria-live="polite"`, placeholder `0`
  - [x] 3.2 `<div id="result-display">` com `aria-live="polite"`, placeholder `0`

- [x] Task 4: Grid de botões (AC: 4)
  - [x] 4.1 `.keypad` com CSS Grid 4 colunas × 5 linhas; 18 botões
  - [x] 4.2 `data-type` e `data-value` em todos os botões
  - [x] 4.3 `aria-label` em operadores (÷, ×, −, +, =), C, ⌫ e decimal

- [x] Task 5: Bloco de histórico (AC: 5)
  - [x] 5.1 `<section class="history-panel">` com `<h2>Histórico</h2>`
  - [x] 5.2 `<ul id="history-list">` com `<li class="history-empty">Nenhuma operação ainda</li>`
  - [x] 5.3 `<button id="clear-history">Limpar histórico</button>`

- [x] Task 6: CSS Custom Properties base (AC: 7)
  - [x] 6.1 `:root` com tokens dark mode completos (bg, text, accent, border, btn, radius, transitions)
  - [x] 6.2 `html.light` com tokens light mode completos
  - [x] 6.3 `font-family` global via `var(--font-family)` aplicada no `body`

- [x] Task 7: Verificação final (AC: 6)
  - [x] 7.1 Script de verificação: 25/25 checks passando (ACs 1–7 cobertas)
  - [x] 7.2 Layout 2 colunas + estrutura HTML5 semântica confirmados

## Dev Notes

### Stack Técnica

- **HTML:** HTML5 semântico, zero frameworks
- **CSS:** Vanilla CSS com Custom Properties, sem bibliotecas externas
- **JS:** ES6+, arquivo `script.js` separado
- **Fonte:** Google Fonts CDN — Inter ou Outfit (weights 300, 400, 700)
- **Persistência nesta story:** Nenhuma (apenas estrutura)

### Estrutura de Arquivos Esperada

```
calculadora-web/           ← raiz do projeto
├── index.html
├── style.css
└── script.js
```

> Nota: Sem build step, sem bundler, sem transpilação. Tudo abre diretamente no browser.

### Paleta de Tokens CSS (Dark Mode Padrão)

```css
:root {
  --bg-primary: #0f0f1a;
  --bg-secondary: #1a1a2e;
  --bg-card: rgba(255, 255, 255, 0.05);
  --text-primary: #ffffff;
  --text-secondary: rgba(255, 255, 255, 0.6);
  --accent-color: #00d4ff;
  --border-color: rgba(255, 255, 255, 0.1);
  --font-family: 'Inter', 'Outfit', sans-serif;
}

.light {
  --bg-primary: #f0f2f5;
  --bg-secondary: #ffffff;
  --bg-card: #ffffff;
  --text-primary: #1a1a2e;
  --text-secondary: rgba(26, 26, 46, 0.6);
  --accent-color: #6c63ff;
  --border-color: rgba(26, 26, 46, 0.1);
}
```

### Estrutura HTML Esperada (esqueleto)

```html
<body>
  <main class="app-container">
    <section class="calculator">
      <div class="display">
        <div id="expression-display">0</div>
        <div id="result-display">0</div>
      </div>
      <div class="keypad">
        <!-- botões -->
      </div>
    </section>
    <section class="history-panel">
      <h2>Histórico</h2>
      <ul id="history-list">
        <li class="history-empty">Nenhuma operação ainda</li>
      </ul>
      <button id="clear-history">Limpar histórico</button>
    </section>
  </main>
</body>
```

### Testing

- Validação manual: abrir `index.html` no Chrome/Firefox e verificar DevTools Console (aba Console + aba Network)
- Não há suite de testes automatizados nesta story
- ACs verificados visualmente pelo `@qa`

## Change Log

| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-05-08 | 1.0.0 | Story criada | River (@sm) |
| 2026-05-08 | 1.1.0 | Validado GO (8/10) — Status: Draft → Ready | Pax (@po) |
| 2026-05-08 | 1.2.0 | Implementação concluída — Status: InProgress → InReview | Dex (@dev) |
| 2026-05-08 | 1.3.0 | QA Gate PASS (95/100) — refactoring aplicado | Quinn (@qa) |
| 2026-05-08 | 1.4.0 | Status: InReview → Done — pushed to origin | Gage (@devops) |

## Dev Agent Record

### Agent Model Used

claude-sonnet-4-6 (Dex / @dev)

### Debug Log References

Nenhuma issue encontrada durante implementação.

### Completion Notes List

- `=` implementado com `grid-row: span 2` no CSS Grid — ocupa linhas 4–5 da coluna 4
- `0` implementado com `grid-column: span 2` — ocupa colunas 1–2 da linha 5
- Botão `−` exibe U+2212 (sinal de menos tipográfico) mas `data-value="-"` (hífen-minus) para aritmética
- `script.js` criado como placeholder com comentário de estrutura; lógica vem na Story 1.2
- Seletor de light mode: `html.light` (consistente com o toggle da Story 3.2 que adiciona/remove classe no `<html>`)
- Tokens CSS incluem variáveis extras (`--btn-hover-bg`, `--btn-active-bg`, `--transition-*`) antecipando Stories 3.1–3.2 sem violação de escopo (apenas Custom Properties, sem regras de animação)

### File List

- `calculadora-web/index.html` — criado
- `calculadora-web/style.css` — criado
- `calculadora-web/script.js` — criado (placeholder)

## QA Results

### Review Date: 2026-05-08

### Reviewed By: Quinn (@qa) — Test Architect

### Code Quality Assessment

Implementação sólida e bem estruturada. HTML5 semântico correto com uso adequado de `<main>`, `<section>`, `<h2>` e atributos ARIA. CSS Custom Properties organizadas e cobrindo todos os tokens necessários para ambos os temas. Layout de 2 colunas funcional com CSS Grid para o teclado e `grid-row: span 2` para o botão `=`. CodeRabbit CLI não disponível neste ambiente — degradação graciosa aplicada conforme `core-config.yaml`.

### Refactoring Performed

- **File:** `calculadora-web/style.css`
  - **Change:** Adicionados tokens `--danger-color`, `--danger-border`, `--display-bg` em `:root` (dark) e `html.light`, com substituição das 3 ocorrências de valores hardcoded nos seletores `.display`, `.btn--clear` e `#clear-history:hover`.
  - **Why:** `.display` usava `rgba(0,0,0,0.2)`, `.btn--clear` e `#clear-history:hover` usavam `#ff6b6b` e `rgba(255,107,107,0.3)` fora do sistema de tokens — isso quebraria a coerência visual no light mode (Story 3.2).
  - **How:** Dois novos tokens por bloco de tema: `--danger-color` (cor do botão destrutivo) e `--display-bg` (fundo do display), com valores semanticamente corretos para dark e light.

### Compliance Check

- Coding Standards: ✓ Vanilla HTML/CSS/JS sem frameworks, conforme PRD NFR2
- Project Structure: ✓ Arquivos em `calculadora-web/` com 3 arquivos separados
- Testing Strategy: ✓ Verificação manual + script de 25 checks (sem suite automatizada, conforme PRD)
- All ACs Met: ✓ 7/7 ACs cobertos e verificados

### Improvements Checklist

- [x] Refatorado: 3 cores hardcoded → tokens CSS (`--danger-color`, `--danger-border`, `--display-bg`)
- [x] Verificado: todos os 7 ACs implementados corretamente
- [x] Verificado: acessibilidade — `aria-live`, `role="group"`, `aria-label`, `:focus-visible`

### Security Review

Sem superfície de ataque relevante nesta story. HTML estático, sem formulários processados, sem inputs enviados a servidor, sem eval(), sem armazenamento de dados sensíveis.

### Performance Considerations

Google Fonts carregado com `<link rel="preconnect">` para otimizar DNS lookup. CSS e JS servidos localmente. Sem JavaScript na carga inicial (script.js é placeholder). Nenhuma preocupação de performance nesta story.

### Files Modified During Review

- `calculadora-web/style.css` — adicionados tokens e substituídas cores hardcoded

> @dev: por favor adicione `calculadora-web/style.css` como "modificado durante QA" no File List da story.

### Gate Status

Gate: **PASS** → `docs/qa/gates/1.1-estrutura-html-layout-base.yml`
Quality score: 95/100
Risk profile: N/A (sem riscos identificados)
NFR assessment: security PASS · performance PASS · reliability PASS · maintainability PASS

### Recommended Status

✓ **Ready for Done** — todos os ACs atendidos, refactoring aplicado, nenhum item pendente.
